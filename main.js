var typed = new Typed(".text", {
    strings:["Fullstack Developer" , "YouTuber" , "Web Developer"],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
});

//make the following change in your index.js file

app.listen(process.env.PORT || 3000);